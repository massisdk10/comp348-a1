// A FAIRE AU COMPLET, LE CODE CEST JUSTE UN TEST FAIT TOUTE CETTE PARTIE 

#include <stdio.h>

void dict_modif(FILE *in)
{
    /* Temporary stub so the project links.
       Teammate will replace with real dict mode. */
    int c;
    while ((c = fgetc(in)) != EOF)
    {
        putchar(c);
    }
}